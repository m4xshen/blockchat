# MCP Server Environment Variables
# Copy this file to .local.env and fill in your values

# Wallet private key (without 0x prefix)
# This is used for signing transactions in tools that require a private key
WALLET_PRIVATE_KEY=your_private_key_here

# Optional: Default network to use if not specified
# DEFAULT_NETWORK=ethereum
